R = 0
N = 0

for n in range(1, 1000):
    s = bin(n)[2:]
    if n % 2 == 0:
        s = s.replace('0', '1')
    else:
        if len(s) > 1:
            s = s[0] + s[1:].replace('1', '00')
    r = int(s, 2)
    if r <= 600 and r >= R:
        R = r
        N = n

print(N)
